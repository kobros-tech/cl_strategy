__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.ruff_cache/

# Generated by skill_memory/tests/demo_splitmnist_eval_log.py -- a
# per-sample eval log, easily >1 MB for a full SplitMNIST run. Never commit
# it; if pre-commit's check-added-large-files already flagged a tracked
# copy, run `git rm --cached <path>` once to untrack it.
logs/
data/
*.csv
